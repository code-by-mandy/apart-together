import { useState } from 'react';

function Form() {
    return(
        <form>
            <textarea>

            </textarea>
            <button>Submit your story</button>
        </form>
    )
}

export default Form;